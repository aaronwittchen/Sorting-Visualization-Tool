// speedConfig.js
// Contains speed configuration for the sorting visualization tool.

// speedMap: Maps speed names to delay values in ms
export const speedMap = {
    "slow": 128,
    "medium": 32,
    "fast": 8
};

// DEFAULT_BUCKET_SIZE: Used for bucket sort
export const DEFAULT_BUCKET_SIZE = 5;
